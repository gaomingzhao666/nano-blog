---
import { cn } from '@/utils'

export interface Heading {
  depth: number
  text: string
  slug: string
  subheadings: Heading[]
}

interface Props {
  heading: Heading
}

const { heading }: Props = Astro.props
---

<li class="">
  <a
    href={'#' + heading.slug}
    class={cn(`py-2 px-4 mb-2 first-letter:uppercase w-fit line-clamp-2`)}
  >
    {heading.text}
  </a>

  {
    heading.subheadings.length > 0 && (
      <ul class="ml-4">
        {heading.subheadings.map((subheading) => (
          <Astro.self heading={subheading} />
        ))}
      </ul>
    )
  }
</li>
